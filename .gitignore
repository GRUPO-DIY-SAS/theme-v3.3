.DS_Store
*.log
CLAUDE.local.md

# Claude Code / Codex: ignore new config (e.g. ECC tooling).
# Files already committed before this rule stay tracked — git ignores
# .gitignore for tracked paths. Use `git add -f` to commit a new one on purpose.
.claude/
.codex/

# IDE / OS
.vscode/
.idea/
